<%= form_tag "videos/#{ @video.id }", method: :patch do  %>

  <div>
    <%= f.label_tag :title %>
    <%= f.text_field_tag :title, @video.title %>
  </div>

  <div>
    <%= f.label_tag :url %>
    <%= f.text_field_tag :url, @video.url %>
  </div>
  
  <%= submit_tag "Submit" %>
<% end %>